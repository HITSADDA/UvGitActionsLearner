name: Context Variables

on:
  workflow_dispatch:

jobs:
  job-1:
    name: getting context variables
    runs-on: ubuntu-latest
    steps:
      - name: Print Context Variables
        run: echo "repo name is ${{github.repository}}"
